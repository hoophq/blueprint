# dbcensus

A read-only census of every managed database reachable from your AWS credentials — RDS, Aurora, DocumentDB, Neptune, DynamoDB, ElastiCache, Redshift. Runs locally, output stays local, zero telemetry.

Full README coming with v0.1. Try it: `go run . scan --demo`
